import { Employee } from '../../types/Employee';

/**
 * An array of mock employee data used for testing purposes.
 * Each object in the array represents an employee with properties such as `firstName`, `lastName`, `dateOfBirth`, `startDate`, `street`, `city`, `state`, `zipCode`, and `department`.
 * The data is randomly generated for each field.
 */
export declare const testerData: Employee[];
